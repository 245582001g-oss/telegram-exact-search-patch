# v1.4.2 / Telegram 7.2.8 验证

机器可读记录见 [verification.json](verification.json)。

- 官方输入 SHA-256：`4de51db14afe0aace6b38665e1110f0eecb1112c387d44f380236b33d963c13d`。
- r8 输出 SHA-256：`b955fe3cc84fd0c65086658f549ea6d03d561273a574783ad70db297d8acf337`。
- 原始节、导入、ASLR/NX、重定位、异常展开表和 PE 校验和通过；Windows 内核 SEC_IMAGE 接受该映像。
- 原有搜索 hook 地址不变。菜单槽仍复制 ID/文本；Qt 对象销毁后不再刷新失效页面。菜单无主窗口的问题由同一界面线程中的稳定主窗口处理。

## 统一回归

实际编译 x64 代码在两个加载地址运行，Qt/Telegram 和文件系统服务使用受控替身：

- 精确搜索、多词 AND、多语言正反例、菜单、全文/频道/关键词黑名单、普通/预览结果、后续分页和注入消息通过。
- 关键词涵盖 91 个频道、改变填充文案、正文/名称两种范围、普通用户和群组保留、字面量语义、损坏文件保护、容量限制、原子保存失败以及 12,000 字符重复前缀负例。
- 多选快照按固定频道 ID 合并，保留同名不同 ID 的频道，排除普通用户/群组；原消息及菜单变化后，复制的名称、示例和 ID 仍有效。
- 1,000 个频道的整批合并、重复 ID、已有顺序保留、非法末尾 ID、临时文件失败和损坏数据库均通过；失败不留下部分提交。
- 多选取消无写入/刷新；提交后才进入可编辑关键词提示。对象销毁和槽释放回归通过。
- PowerShell 5.1 管理器 94 项、安装/恢复 40 项、差分重建 24 项、Python 差分包 14 项和未知输入写入前拒绝检查通过。

## 真实原生窗口

隔离测试使用真实 Win32 控件和临时文件，不登录账号：

- 有主及无主的临时菜单自动销毁后，关键词与多选窗口仍保留；弹窗绑定稳定主窗口，打开时禁用主窗口，关闭后恢复。
- 关键词预填、中文/emoji 编辑、保存、跳过、范围切换、列表、删除和 PowerShell 工具互通通过。
- 240 行真实复选框列表覆盖勾选、右键连续选取/取消、双向边缘自动滚动、跨页保留、全选/清空、取消无写入和准确保存所选 ID。
- 重复调用保留同一窗口、勾选及未保存文字；取消后在同一进程第二次打开可用，没有残留忙状态。

## 本机验证与边界

本机已正常退出旧版并安装 r8，重开后响应正常、完整哈希一致、原有规则字节保持不变，运行中自动更新设置为 0。实际账号的菜单已核对打开、取消、再次打开及稳定父窗口关系；此次代理检查没有添加或删除个人规则。

整批保存和过滤的正反例由隔离原生窗口与 x64 回归覆盖，不将它们描述成真实联网账号的大规模屏蔽验收。全新配置的欢迎页和简体中文切换在 r6 检查通过；r8 未改动启动及语言逻辑，但这一项不冒充 r8 的全新配置重复实测。

## 打包隐私

完整包从已校验的官方便携 ZIP、r8 构建和固定白名单生成；不读取已登录安装目录。tdata、登录信息、个人规则、聊天、缓存、日志及本机截图不纳入发布。
